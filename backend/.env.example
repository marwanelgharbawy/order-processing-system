DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=bookstore
DB_PASS=replace-with-your-local-database-password
DB_NAME=BookstoreDB
PORT=3000
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=replace-with-a-random-session-secret
